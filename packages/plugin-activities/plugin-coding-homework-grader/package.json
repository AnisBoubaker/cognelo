{
  "name": "@cognelo/plugin-coding-homework-grader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@cognelo/activity-sdk": "0.1.0",
    "@prisma/client": "^5.14.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "db:generate": "set -a && . ../../../.env && set +a && prisma generate --schema prisma/schema.prisma",
    "typecheck": "tsc -p tsconfig.json"
  }
}
