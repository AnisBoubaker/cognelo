{
  "name": "@cognelo/plugin-placeholder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@cognelo/activity-sdk": "0.1.0",
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "db:generate": "set -a && . ../../../.env && set +a && prisma generate --schema prisma/schema.prisma",
    "typecheck": "tsc -p tsconfig.json"
  }
}
