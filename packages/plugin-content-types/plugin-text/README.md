# Plugin: Text Content

`@cognelo/plugin-text-content` owns simple text/Markdown course content.

It provides picker metadata, text settings UI, body validation, and embedding source hints. The first implementation stores the Markdown body in generic `CourseContentResource.metadata`.
