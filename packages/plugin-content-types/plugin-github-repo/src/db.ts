export const githubRepoContentDatabaseModule = {
  namespace: "plugin_github_repo_content",
  tables: [],
  notes: ["GitHub repo content currently stores safe source metadata in CourseContentResource.metadata."]
} as const;
